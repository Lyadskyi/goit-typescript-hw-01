// JavaScript масив:

// let person = ["Max", 21];

// TypeScript з використанням концепції кортежів, де перший елемент завжди буде рядком, а другий - числом:

let person: [string, number];

person = ["Max", 21];

console.log(person); // Виведе: ["Max", 21]
